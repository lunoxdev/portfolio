---
import data from "../data/data";
---

<section class="space-y-10">
  <h2 class="font-semibold text-4xl">Projects</h2>
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
    {
      data.projects.map((info) => (
        <div class="flex flex-col relative pb-24 space-y-4">
          <figure>
            <img
              src={info.img}
              alt={info.name}
              width="800"
              height="800"
              class="rounded-lg w-full h-40 border"
              loading="eager"
            />
            <figcaption class="font-semibold text-base text-start mt-3">
              {info.name.toUpperCase()}
            </figcaption>
          </figure>

          <p class="text-sm text-gray-400">{info.description}</p>

          <a
            href={info.url}
            target="_blank"
            rel="noreferrer nofollow"
            class="py-3 border-b-4 border-t-4 rounded-lg border-blue-600 w-full text-center hover:bg-[#061A40] hover:font-semibold absolute bottom-0"
          >
            Explore {info.btnname}
          </a>
        </div>
      ))
    }
  </div>
</section>
